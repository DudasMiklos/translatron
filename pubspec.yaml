name: translatron
description: !BETA VERSION - NOT FOR LIVE OR PROD USAGE!  A package for translations, from api calls. Only use if your backend team can provide the nedded api urls!
version: 0.1.0
homepage: https://dudasmiklos.com/
repository: https://github.com/DudasMiklos/translatron
documentation: https://dudasmiklos.com/

environment:
  sdk: '>=2.19.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  path_provider: ^2.1.1
  shared_preferences: ^2.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
